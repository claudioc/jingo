extends layout

block content
  h3 #{title} 

  table.compare.table.table-bordered.table-hover.table-condensed
    each line in lines
      tr
        td.ln #{line.ldln}
        td.ln #{line.rdln}
        td(class="#{line.class}") #{line.text}
